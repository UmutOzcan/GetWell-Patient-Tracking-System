import * as React from 'react';
import {View, Text} from 'react-native';

const ExerciseView = () => {
  return (
    <View style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
      <Text style={{fontSize: 50}}> UMUT </Text>
    </View>
  );
};

export default ExerciseView;
